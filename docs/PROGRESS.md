# Прогресс PriceBot

## Сейчас

Этап E закрыт проверкой. Контур A–E по ТЗ готов к локальному `docker compose up` и ручному прогону с живым `BOT_TOKEN`.

## Сделано

- 2026-09-12 `docs/TZ.md` — закон продукта.
- 2026-09-12 **этап A закрыт проверкой:** домен + тесты.
- 2026-09-12 **этап B закрыт проверкой:** Excel→БД, поиск в боте/API.
- 2026-09-12 **этап C закрыт проверкой:** Mini App, initData, корзина.
- 2026-09-12 **этап D закрыт проверкой:** чекаут, QR, webhook, заказы.
- 2026-09-12 **этап E закрыт проверкой:**
  - админ: заказы/статусы (не `paid`), юзеры/блок, промо, тикеты, выгрузка xlsx
  - промо на чекауте; тикеты в боте + `/reply T-…`
  - Telegram limiter 28/с + retry `retry_after`; лог необработанных ошибок
  - бэкап `pg_dump` + ротация; Compose postgres/redis/api/bot
  - `docs/ADMIN.md`; нагрузка 1000 одновременных GET поиска

## Дальше

Ручной прогон с `.env`: `docker compose up --build`. ЮKassa — ключи из кабинета. Коммит исходников — по запросу пользователя (этот прогон).

ЮKassa live: нужны `YOOKASSA_SHOP_ID` / `YOOKASSA_SECRET_KEY` из кабинета (не выдумывать). Закрытие нового этапа в таблице — у родителя.

## Заблокировано

Нет. Живой Telegram нужен только для ручного прогона, не для автотестов.

## Проверки

| Этап | Дата | Команда | Результат |
|------|------|---------|-----------|
| A | 2026-09-12 | `uv run pytest -q` | **49 passed** |
| B | 2026-09-12 | `uv run pytest -q` | **70 passed** |
| C | 2026-09-12 | `uv run pytest -q` / браузер | **79 passed** / UI ok |
| D | 2026-09-12 | `uv run pytest -q` | **88 passed** |
| E | 2026-09-12 | `uv run pytest -q` | **100 passed** |
| E | 2026-09-12 | `uv run ruff check pricebot tests` | All checks passed |
| E | 2026-09-12 | `uv run python scripts/load_search.py --n 1000` | N=1000 concurrent, **5xx=0**, p50=4414.58 ms, p95=7460.46 ms |
